function Chai(){
    //NAMING CONVENTION::FUNCTION  NAME SHOULD START FROM UPER CASE ONLY
    //THIS NAMING CONVENTION SHOULD FOLLOWED IN CREAT-REACT-APP METHOD ALSO
    return(
        <>
        <h2>returning from new file</h2>
        <h4>Trying ANOTHER ELEMENT</h4>
        </>
        //WE HAVE TO WRAP IN <></>(CALLED FRAGMENT) OR <DIV></DIV>  IF WE WANT TO EXPORT NORE THAN ON ELEMENT 
        //BECAUSE JSX ALLOWS ONLY ONLE ELEMENT TO BE EXPORTED
    )
}

export default Chai

//some libraries like vite force to make extension .jsx (Halwa.jsx) of your component 